Feature: Admin login
Scenario: Login successful

     When I set "string" to the inputfield "xpath"
     And I set "string" to the inputfield "xpath"
     When I click on the element "xpath"
     Then I expect that element "xpath" contains the text "tex"
